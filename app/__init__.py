"""Package initializer for the `app` module."""

# This file makes `app` a Python package. It doesn't need to expose anything
# right now, but having it allows us to import modules from tests or other
# places if needed.